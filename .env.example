PORT=8080

DB_HOST=localhost
DB_PORT=5432
DB_USER=streaming
DB_PASSWORD=streaming
DB_NAME=streaming
DB_SSLMODE=disable

# Optional override; when provided it takes precedence over DB_* fields.
# DATABASE_URL=postgres://streaming:streaming@localhost:5432/streaming?sslmode=disable

MIGRATIONS_DIR=migrations
